{
  "name": "dhronepredicts-management-system",
  "version": "1.0.0",
  "description": "Admin dashboard to manage football predictions stored in JSON files",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
-------
    "build": "cd client && npm install && npm run build",
    "install-client": "cd client && npm install",
    "install-all": "npm install && npm run install-client",
    "netlify-build": "cd client && npm install && npm run build",
    "heroku-build": "cd client && npm install && npm run build",
    "railway-build": "cd client && npm install && npm run build",
    "prebuild": "npm install"
  },
  "keywords": [
    "football",
    "predictions",
    "admin",
    "dashboard"
  ],
  "author": "DhronePredicts",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "fs-extra": "^11.1.1",
    "uuid": "^9.0.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}